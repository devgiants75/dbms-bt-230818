### view_practice ###

-- 1. 기존에 뷰가 있으면 덮어쓰고, 없으면 새로 생성하는 SQL문
# (create or replace view) 뷰_이름
# as
# 	select 문;

-- 2. 뷰에 대한 설명 중 틀린 것을 고르시오.
# 1. 뷰는 가상의 테이블이라고 부를 수 있다. (O)
# 2. 뷰의 실체는 create table문이다. (select)
# 3. 뷰는 이름 앞에 v_를 붙이는 것이 관례이다. (O)
# 4. 뷰를 만든 후에는 테이블과 동일하게 접근한다. (O)